node_modules
dist
dist-ssr
*.local
.DS_Store
**/.DS_Store
.vscode/*
!.vscode/extensions.json
.idea
*.log

# claude-flow session droppings
.claude-flow/
.swarm/

# Internal documentation: tickets, Figma links, translation source, decision log.
# Canonical copy lives with the project in DevHubVault.
_context/

# Raw motion-designer deliverables (187 MB each) and extracted reference frames.
_refs/

# Everything in public/video/ is a build output of scripts/encode-video.sh —
# the dev proxies of the reference clips AND the shipping encodes. None of it
# belongs in git; regenerate with `npm run video:dev` / `npm run video:prod`.
public/video/*.mp4
public/video/*.webm

# fly:measure intermediates (2 MB of blob tracks); regenerate with npm run fly:measure
.fly-cache/
